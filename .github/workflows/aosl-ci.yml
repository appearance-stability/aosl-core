name: aosl-ci

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  smoke:
    name: aosl-ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: AOSL CI placeholder
        run: echo "AOSL CI is alive."
